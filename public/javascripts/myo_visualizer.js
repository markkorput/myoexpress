// Generated by CoffeeScript 1.6.3
(function() {
  this.MyoVisualizer = (function() {
    function MyoVisualizer(_opts) {
      var _this = this;
      this.options = _opts;
      this.scene = this.options.scene;
      this.myo_recorder = this.options.myo_recorder;
      this.meshes = [];
      this.myo_recorder.on('add', function(myo_record) {
        var geometry, material, mesh, orientation;
        orientation = myo_record.get('orientation');
        if (!orientation) {
          return;
        }
        geometry = new THREE.SphereGeometry(50, 10, 10);
        material = new THREE.LineBasicMaterial();
        mesh = new THREE.Mesh(geometry, material);
        mesh.position.set(orientation.x, orientation.y, orientation.z);
        mesh.position.multiply(new THREE.Vector3(500, 500, 500));
        _this.meshes << mesh;
        return _this.scene.add(mesh);
      });
    }

    return MyoVisualizer;

  })();

}).call(this);
